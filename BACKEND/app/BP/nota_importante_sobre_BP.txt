    1. Tiene que estudiar sobre como se utilizan BluerPrints en Flask, con esto podremos hacer
       el codigo mantenible.
    2. Es importante que cuando trabajen con su entorno virtual lo creen con este nombre env


    estos son los pasos:
    Paso 1: se crea el entorno desde C:\Users\Abc\Desktop\...\Modulo de Compras> 
            una vez se encuentra en ese directorio ejecutan python -m venv env

    Paso 2: navegando entre los directorios se activa el entorno virtual asi: env\Scripts\activate

    - Instalar desde requirements:
                                    pip install -r requirements.txt
    
    - Actualizar el requirements
                                    pip freeze > requirements.txt
 